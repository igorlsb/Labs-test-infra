worker_processes 1;

events {
    worker_connections 1024;
}

http {
    include /etc/nginx/mime.types;
    default_type application/octet-stream;

    sendfile on;
    keepalive_timeout 65;

    # Redirecionamento HTTP para HTTPS
    server {
        listen 80;
        server_name frontendtest626.com;

        location / {
            return 301 https://$host$request_uri;
        }
    }

    # Servidor HTTPS com Certificado Autoassinado
    server {
        listen 443 ssl;
        server_name frontendtest626.com;

        ssl_certificate /etc/letsencrypt/live/frontendtest626.com/fullchain.pem;
        ssl_certificate_key /etc/letsencrypt/live/frontendtest626.com/privkey.pem;

        ssl_protocols TLSv1.2 TLSv1.3;
        ssl_ciphers HIGH:!aNULL:!MD5;

        location / {
            proxy_pass http://frontendtest626:80;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto https;
        }
    }
}
